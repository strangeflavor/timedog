#define form_mirror
///form_mirror()
trace('starting form_mirror');

var arg;
for (var i = 0; i < 16; i += 1;) if argument_count > i arg[i] = argument[i] else arg[i] = -1;

make(oMirror);

#define form_2mirror
///form_2mirror()
trace('starting form_2mirrors');

var arg;
for (var i = 0; i < 16; i += 1;) if argument_count > i arg[i] = argument[i] else arg[i] = -1;

with make(oMirror) _xo = -40;
with make(oMirror) _xo = 40;
